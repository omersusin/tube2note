# LEGACY — HF Spaces Docker now requires PRO. Use Dockerfile.backend
# (Render/Fly) for free hosting. Kept so old deploys don't 404.
FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir ".[pdf]"
CMD ["python", "-m", "tube2note", "serve"]
